SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
CREATE PROCEDURE [dbo].[USP_RO_REPORT_BYPAYMENTMODE]
@date DATETIME, 
@SPMID INT
AS
BEGIN
SELECT 
SUM(sm.NetAmount)
 FROM dbo.RO_SalesMaster sm WHERE CAST(BillDate AS DATE)=CAST(@date AS DATE) and sm.SPMID = @SPMID
END	





GO
